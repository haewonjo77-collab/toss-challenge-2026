# 6인 회의 일정 잡기 UX — 프로젝트 스펙

토스 프로덕트 디자이너 챌린지 2026 과제. 6명(주최자 1 + 참석자 5)이 1주일 내 1시간 회의를 잡는 경험 설계.

## 문제 정의

> 필수와 선택을 구분해도, 추천은 여전히 인원수로만 매겨진다.

기존 캘린더/투표 도구는 참석자의 됨/안됨을 모으는 것까지는 하지만, 그중 누가 꼭 필요한 사람인지(필수/선택)는 최종 추천에 반영하지 않는다.

## 해결 가설 3개

| | 문제 | 해결책 | 개선되는 것 |
|---|---|---|---|
| A (보조) | 참석자 응답이 서로에게 안 보여 확정이 늦어짐 | 응답 현황을 서로에게 공개(오픈폴 방식) | 확정까지 걸리는 시간 |
| B (보조) | 한 번에 너무 많은 날짜·시간을 보여줘 응답 부담이 큼 | "안 되는 시간"만 표시 + 단계적 노출 | 응답 완료율·속도 |
| **C (핵심)** | 추천 기능이 있어도 인원수만 셀 뿐 필수/선택은 반영 안 함 | 필수 전원가능 우선 + 선택 겹침도 순 추천 | 확정까지 걸리는 클릭 수·시간 |

C가 핵심 기능, A/B는 C가 잘 작동하도록 돕는 보조 장치.

## 기능 및 필요 데이터

| 기능 | 담당 | 필요한 데이터 |
|---|---|---|
| 응답 현황판 | A | 참석자별 이름, 응답여부(됨/안됨) |
| 됨/안됨 입력폼 (단계적 노출) | B | 참석자별 안되는 날짜·시간 목록 |
| 필수/선택 기반 추천 로직 | C | 참석자별 필수/선택 태그(주최자가 지정), 각자의 됨/안됨 목록 |

## 유저플로우

### 메인 플로우 (주최자)
```
① 회의 만들기
   - 제목, 시간 1시간(고정)
   - 참석자 초대 + 각자에게 필수/선택 태그 지정

② 대기 화면
   - 응답 현황판: "O명 중 O명 응답완료" + 참석자별 이름·응답여부만 표시
   - (응답시각 없음 — 불필요한 데이터로 판단해 제외)

③ 추천 결과 확인
   - CASE 1 (필수 전원가능 시간 있음): 최상단 추천, 배지 "필수 6/6, 선택 2/3"
   - CASE 2 (필수 전원가능 시간 없음): "필수 5/6" 등 가장 가까운 후보를
     "차선책"으로 표시, 빠지는 사람 명시 + (선택) 재확인 요청 버튼

④ 확정
   - 후보 하나 클릭 → 즉시 확정 (재확인 단계 없음, 클릭 수 최소화)
```

### 보조 플로우 (참석자)
```
① 초대 링크 접속 (로그인 불필요)
② "안 되는 시간을 표시해주세요" — 날짜를 며칠씩 나눠서 노출 (한 번에 다 안 보여줌)
③ 제출 → 현재 응답 현황(요약) 확인 가능
```

## 디자인 원칙

- **UI는 최소한으로, UX가 튀도록.** 브랜딩/비주얼 컨셉 평가 대상 아님 — 여백·타이포·상태 피드백 위주로 절제된 완성도.
- 익숙한 UI 패턴(캘린더 그리드, 체크박스, 배지)을 맥락에 맞게 다듬어 쓰기. 새로 발명하지 않기.
- 사용자에게 "왜 안되는지" 이유를 묻지 않음 — 됨/안됨, 필수/선택 표시만으로 충분.

## 범위 밖 (이번엔 다루지 않음)

- 회의 카테고리/고정 멤버 재사용 기능 — 확장 아이디어로 보류. 핵심 플로우 목업 완성 후 시간 남으면 추가 검토.
- 브랜딩, 전체 앱 플로우, 로그인/회원가입
- 익명투표, 복수선택 등 옵션 기능 (6명 소규모 시나리오엔 불필요 판단)

## 검증 방법

목업 사용성 테스트에서, "필수/선택 배지가 있는 화면 vs 없는 화면"을 비교해 주최자가 최종 시간을 확정하기까지 걸리는 클릭 수·시간 차이를 측정.
